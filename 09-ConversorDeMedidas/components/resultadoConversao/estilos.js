import { StyleSheet } from "react-native";

const estilo = StyleSheet.create({
    areaResultado: {
        marginTop: 35,
        paddingVertical: 60,
    },
    resultado: {
        fontSize: 120,
        color: '#fff',

        textAlign: 'center',
    },
  });
  
  export default estilo;